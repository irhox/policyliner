##########################################################################################################
Disclosure Policies used in the Scenarios in section 5.2 in the Experiments' Chapter of my master thesis
##########################################################################################################
1. Enhanced Public Health Researcher Policy
disclose
    patientdateofbirth,
    patientrace,
    patientmartialstatus,
    patientgender,
    patientpopulationpercentagebelowpoverty,
    admissionstartdate,
    admissionenddate,
    primarydiagnosiscode,
    from patients, admissions, diagnosis
    with mask on patientdateofbirth using bucketize_age('5 years')
    with mask on patientrace using randomized_response_patientrace()
    with mask on patientmartialstatus using randomized_response_patientmaritalstatus()
    with mask on patientgender using randomized_response_patientgender()
    with mask on patientpopulationpercentagebelowpoverty using bucketize(5)
    with mask on primarydiagnosiscode using generalize_primarydiagnosiscode()
    with mask on to_date(to_char(admissionstartdate, 'DD/MM/YYYY'), 'DD/MM/YYYY') using generalize_date('MONTH')
    with mask on to_date(to_char(admissionenddate, 'DD/MM/YYYY'), 'DD/MM/YYYY') using generalize_date('MONTH')
    where patients.patientid = admissions.patientid
    and admissions.patientid = diagnosis.patientid
    and $user.role = 'public_health_researcher'



#############################################################################################################
Examples of other Disclosure Policies
#############################################################################################################

1. Front Desk Staff
disclose
    patients.patientid,
    patientgender,
    patientmartialstatus,
    patientlanguage,
    admissions.admissionid,
    admissionstartdate,
    admissionenddate
    from patients, admissions
    with mask on to_date(to_char(admissionstartdate, 'DD/MM/YYYY'), 'DD/MM/YYYY') using generalize_date('MONTH')
    with mask on to_date(to_char(admissionenddate, 'DD/MM/YYYY'), 'DD/MM/YYYY') using generalize_date('MONTH')
    with mask on patientgender using randomized_response_patientgender()
    with mask on patientmartialstatus using randomized_response_patientmaritalstatus()
    with mask on patientlanguage using randomized_response_patientlanguage()
    where patients.patientid = admissions.patientid
    and $user.role = 'front_desk_staff'

2. Lab Technician
disclose
    labname,
    labvalue,
    labunits,
    labdatetime
    from labs
    with mask on labname using generalize_labname()
    with mask on to_date(to_char(labdatetime, 'DD/MM/YYYY'), 'DD/MM/YYYY') using generalize_date('MONTH')
    where $user.role = 'lab_technician'

3. Specialist Doctor
disclose
    patients.patientid,
    admissions.admissionid,
    admissionstartdate,
    admissionenddate,
    primarydiagnosiscode,
    primarydiagnosisdescription
    from patients, admissions, diagnosis
    with mask on patients.patientid using generalize_primarydiagnosiscode()
    with mask on admissions.admissionid using bucketize(5)
    with mask on primarydiagnosiscode using generalize_primarydiagnosiscode()
    with mask on to_date(to_char(admissionstartdate, 'DD/MM/YYYY'), 'DD/MM/YYYY') using generalize_date('MONTH')
    with mask on to_date(to_char(admissionenddate, 'DD/MM/YYYY'), 'DD/MM/YYYY') using generalize_date('MONTH')
    where patients.patientid = admissions.patientid
    and admissions.patientid = diagnosis.patientid
    and $user.role = 'specialist_doctor'

4. Patient Demographics
disclose
    patientid,
    patientgender,
    patientdateofbirth,
    patientrace,
    patientmartialstatus,
    patientlanguage
    from patients
    with mask on patientgender using randomized_response_patientgender()
    with mask on patientmartialstatus using randomized_response_patientmaritalstatus()
    with mask on patientlanguage using randomized_response_patientlanguage()
    with mask on patientrace using randomized_response_patientrace()
    where $user.role = 'patient_demographics'

5. Diagnoses per Admission
disclose
    admissions.admissionid,
    admissions.patientid,
    primarydiagnosiscode,
    primarydiagnosisdescription
    from admissions, diagnosis
    with mask on admissions.patientid using generalize_primarydiagnosiscode()
    with mask on primarydiagnosiscode using generalize_primarydiagnosiscode()
    where admissions.patientid = diagnosis.patientid
    and $user.role = 'diagnoses_per_admission'